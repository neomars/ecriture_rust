{
  "scripts": {
    "postinstall": "npm install --prefix ecriture-rust",
    "tauri": "npm run tauri --prefix ecriture-rust --"
  },
  "dependencies": {
    "jsdom": "^29.1.1",
    "playwright": "^1.63.0"
  }
}
